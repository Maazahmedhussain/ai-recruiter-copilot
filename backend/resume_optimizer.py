def generate_resume_suggestions(missing_skills):

    suggestions = []

    for skill in missing_skills:

        suggestions.append(
            f"Implemented enterprise solutions using {skill} to support scalable cloud infrastructure."
        )

        suggestions.append(
            f"Developed automated deployment pipelines utilizing {skill}."
        )

    return suggestions